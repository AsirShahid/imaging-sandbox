# Workstation-only. Do NOT install these on the ~2GB VPS.
# Install the CPU build of torch unless you have a GPU:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements-offline.txt
torch>=2.2
monai>=1.3
highdicom>=0.22
pydicom>=3
numpy>=1.26
SimpleITK>=2.3
httpx>=0.27
